  {/* Hero Section */}
      <div className="text-center mt-8">
        <h2 className="text-3xl font-bold text-gray-800">
          Create Your Own Meme 🚀
        </h2>
        <p className="text-gray-600 mt-2">
          Add funny text and generate your meme instantly!
        </p>
      </div>

      {/* Meme Form */}
      <div className="max-w-xl mx-auto mt-8 bg-white p-6 rounded-xl shadow-md">
        <div className="grid grid-cols-2 gap-4">
          <input
            type="text"
            placeholder="Top Text"
            value={topText}
            onChange={(e) => setTopText(e.target.value)}
            className="border p-2 rounded-md"
          />

          <input
            type="text"
            placeholder="Bottom Text"
            value={bottomText}
            onChange={(e) => setBottomText(e.target.value)}
            className="border p-2 rounded-md"
          />
        </div>

        <button
          onClick={() => setImage(`https://api.memegen.link/images/random.jpg`)}
          className="w-full mt-4 bg-purple-600 text-white py-2 rounded-md hover:bg-purple-700"
        >
          Generate Meme
        </button>
      </div>

      {/* Meme Display */}
      <div className="flex justify-center mt-8">
        <div className="relative">
          <img
            src={image}
            alt="meme"
            className="w-400px rounded-lg shadow-lg"
          />

          <h2 className="absolute top-2 left-1/2 transform -translate-x-1/2 text-white text-xl font-bold drop-shadow-md">
            {topText}
          </h2>

          <h2 className="absolute bottom-2 left-1/2 transform -translate-x-1/2 text-white text-xl font-bold drop-shadow-md">
            {bottomText}
          </h2>
        </div>
      </div>